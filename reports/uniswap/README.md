# Uniswap 分析索引

[← ホーム](../../README.md)

<p>
  <a href="https://uniswap.org/"><img src="../../photo/uniswap-logo.png" alt="Uniswap" height="76"></a>
</p>

| 項目 | 内容 |
|---|---|
| プロトコル | [Uniswap v3](https://uniswap.org/)。集中流動性型 AMM |
| チェーン | Arbitrum One(主)。Ethereum / Base / Avalanche / BSC の生ログも取得 |
| データの出所 | **オンチェーンのログのみ。費用 $0**(公開 RPC、API キー不要)。作業場所は `E:\Uniswap-arb` |
| 状態 | **レポート移設済み。マルチチェーン取得を完遂中** |

---

## この取引所を他と分けて扱う理由

板のある取引所(Hyperliquid / Lighter / Derive / Pendle Boros)と**市場の作りが
根本的に違う**ので、同じ指標でも意味が変わる。

| | CLOB | **Uniswap v3(AMM)** |
|---|---|---|
| 気配 | 観測量。スプレッドは変動する | **スプレッド = 手数料(既知の定数)** |
| 板の数量 | 表示分のみ(隠れ注文は見えない) | **閉形式で厳密**。近似がゼロ |
| 価格の刻み | 取引所が決めるティック | **tickSpacing = 手数料ティアに紐付く**(0.05% なら 10bp) |
| 待ち行列 | 存在する | **存在しない** |
| 板の非対称(OBI) | 主要な予測変数 | **縮退している**(標準偏差が CLOB の 1/15) |
| 参加者の行動 | 口座が見えない | `Mint`/`Burn` の `owner` は見えるが **NFPM で潰れる**(下記) |
| 時間の粒度 | ns | **ブロック**(Arbitrum 0.251 秒)。同一ブロック内の順序は MEV が決める |

---

## レポート

| レポート | 内容 |
|---|---|
| [★AMM 特徴量 — 33 節すべての算出](amm_features.md) | 33 節・80 群を Swap 720 万件から算出(7 種・のべ 519 列・3.5 GB)。★**LP は手数料と同額を LVR で失う** — WETH/USDC 0.05% で手数料 $5,136k 対 LVR $5,357k、差引 **−$221k**(出来高の −0.2155bp)。**別経路**(約定 1 件ごとの markout × 数量)で積むと 300 秒時点で $5,383k と **0.47% 差で一致**。★**Kyle λ は深さの逆数**(両対数で傾き −1.07・r=−0.996・7 桁)。★**Arbitrum に挟み込みは実質存在しない**(被害 0.022%)、**JIT も 0.23%** — 素朴に「同一 tx の mint+burn」で数えると 15.63% で **68 倍まちがえる** |
| [★AMM から CLOB 型 L2 を作る](l2_from_amm.md) | tick 別流動性を CLOB の L2 板と同じ形へ変換(15 プール × 4,328 断面)。段の数量は**閉形式で厳密**なので近似ゼロで、CLOB より精度が高い。★**スプレッドは手数料そのもの**。★★**AMM に「板の非対称」は存在しない** — LI の標準偏差は CLOB の 1/15、\|LI\|>0.1 は 0.6% 対 65.9% |
| [★手数料ティア間のリードラグ](leadlag_fee.md) | WETH/USDC は同じペアで手数料の違う 4 プールがある。**どれが先に動くか**を 181 日で測定。方向は**手数料ではなく活発さ**で決まり **0.05% → {0.01%, 0.30%} → 1.00%** の階層(偏相関 p=1e-15〜1e-27)。機構は**無裁定帯**で、1.00% プールの乖離が −52bp で止まるのは直す費用が 105bp だから |

---

## データの状態

### 主系列 — Arbitrum WETH/USDC 系 15 プール

| | |
|---|---|
| 期間 | 2026-03-10 14:38 〜 2026-09-06 22:49(181 日) |
| 生ログ | 8,009,005 件 |
| イベント | Swap 7,207,236 / Burn 296,547 / Collect 290,093 / Mint 208,498 / Flash 946 |
| 検証 | **板の再構成が Swap 720 万件と完全一致(誤差 0)** |

### マルチチェーン — トークン化株式(RWA)プール

`found_pools.csv` の流動性 > 0 の 14 プール(銘柄 EURC / QQQB / PAXG / NVDA)。

| chain | 取得区間 | 被覆率 | 備考 |
|---|---:|---:|---|
| ethereum | 323 | **100%** | |
| arbitrum | 63 | **100%** | |
| base | 7,813 | **100%** | |
| avalanche | 27,969 | **100%** | |
| bsc | 取得中 | — | ★下記の制約あり |

**★BSC だけ窓が短い。**公開 RPC はほぼ全て `eth_getLogs` を弾く
(publicnode = 403 / dataseed 系 = limit exceeded / drpc・zan・blockpi = 実質レート制限)。
唯一通る `bsc.rpc.blxrbdn.com` も**アーカイブが head−21.06M まで**で、
BSC のブロックが 0.450 秒なので **約 110 日**にしかならない。
他チェーンの 180 日には届かないので、BSC を含む横断比較は窓を揃えること。

**★QQQB は BSC にしか存在しない**(他 4 チェーンにゼロ)ので、BSC を落とすと
銘柄が 1 つ丸ごと消える。短い窓でも取る判断をした。

---

## このデータで原理的に出ないもの

| | 理由 |
|---|---|
| **LP の身元** | `Mint` の 97.3% は owner が NonfungiblePositionManager(`0xc36442b4…fe88`)。プールのイベントはポジションを `(owner, tick_lower, tick_upper)` でしか区別しないので、**NFPM 配下の全 LP が同じ鍵に潰れる**。復元には NFPM の `IncreaseLiquidity` / `Transfer`(tokenId 付き)が別途要る |
| gas | ログに入っていない |
| 他 DEX | 取得していない |

`pos_*` の集計は「**鍵ごと**」であって「LP ごと」ではない。そう呼ばないこと。
